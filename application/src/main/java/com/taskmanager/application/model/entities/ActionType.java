package com.taskmanager.application.model.entities;

public enum ActionType {
    //In a future, this enum can be used to define different types of actions that can be performed on tasks.
    COMMENT,
    EDIT_TASK,
    CREATE_TASK;
}
